// The restaurant shown when a customer opens the app without a specific slug.
// In production this comes from the scanned table QR code / subdomain.
export const DEFAULT_RESTAURANT_SLUG = 'the-golden-fork'
